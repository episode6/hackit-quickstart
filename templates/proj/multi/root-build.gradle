allprojects {
  repositories {{{ range $index, $repo := .ProjectRepos }}
    {{ $repo }} {{ end }}
  }

  group = '{{ .Group }}'
  version = '{{ .Version }}'
}

task clean(type: Delete) {
  delete rootProject.buildDir
}
